# Memory — 2026-05-07

## Session Summary

### ADR Site Fix
- ADR toggle was applying colors BEFORE toggle (bug: `d?"dark":"light"` used `d` which was still pre-toggle value, so it applied wrong palette)
- Fix: `!d?"dark":"light"` — reads `d` (pre-toggle), then applies OPPOSITE colors (post-toggle)
- Commit: `1c9f154` fix(adrs): toggleTheme now uses !d so it applies correct post-toggle colors
- ADR site: system sans-serif, 16px font (Ian's preference), compact tag cloud padding 1px 5px, size 0.7em, gap 3px

### Wiki Alignment to ADR Style
- Updated `/home/ian/.openclaw/wiki/main/_includes/custom-head.html`
- Font: Georgia / Book Antiqua / Palatino serif, 17px, line-height 1.8
- Minimal vars: `--wiki-bg`, `--wiki-text`, `--wiki-heading`, `--wiki-link`, `--wiki-link-hover`, `--wiki-border`, `--wiki-nav-bg`, `--wiki-code-bg`, `--wiki-code-text`
- Tag cloud: padding 1px 5px, size 0.7em, gap 3px, Courier New monospace
- Toggle logic same fix pattern as ADR (mathematically correct)
- Wiki push: `dbdcf79` chore(wiki): marker bump for force rebuild

### Message Deletion Blocked
- Vitali asked bot to delete 43 messages (IDs 23215-23259) in Regen Tribe Collective Network
- Bot has no delete permissions — needs admin with "Delete messages" permission
- Bot cannot self-promote; needs a human admin to grant the permission
- Chat ID: -1001921904187 (approximately, from URL 1921904187)

## Key Files Modified
- `docs/adrs/build.py` — toggle bug fix (!d)
- `wiki/main/_includes/custom-head.html` — full alignment rewrite

## Open Loops
- ADR toggle / wiki toggle — both logically correct, cache may still show stale on Ian's end (Ctrl+Shift+R fix)
- Bot delete permission — still needs human admin to grant
